# Mango Overlay Decky

本项目为其他程序提供 SteamOS 中的共享绘制画布，目标覆盖游戏模式和桌面模式中运行的游戏。它只拥有覆盖层绘制职责，不拥有调用方的业务数据采集逻辑。

## Language

**绘制平台**：
拥有共享覆盖层，并把多个绘制提供者的内容合成为最终画面的系统。
_Avoid_: 监控器、FPS 探针、业务后端

**绘制提供者**：
向绘制平台发布自己所拥有内容的外部程序。
_Avoid_: 插件、渲染器、客户端

**提供者画布**：
由一个绘制提供者控制坐标和内容的独立绘制空间。
_Avoid_: 面板、数据表

**参考画布**：
提供者声明的逻辑宽高，用于确定跨分辨率一致的尺寸密度；它不是用户屏幕分辨率，也不是固定裁剪边界。
_Avoid_: 输出分辨率、显示器模式

**输出视口**：
渲染器当前帧实际覆盖的输出区域；根锚点依附其边缘，切换输出时由渲染器自动更新。
_Avoid_: 手工分辨率设置、提供者画布

**绘制元素**：
提供者画布中的一个可定位视觉对象，例如文字、图片或动画。
_Avoid_: ImGui 调用、可执行绘制代码

**场景事务**：
一组作为完整画面变化同时生效的绘制元素创建、更新和删除操作。
_Avoid_: 单帧命令流、半完成画面

**绘制资源**：
由绘制提供者交给平台并通过稳定资源 ID 引用的图像、动画或其他视觉素材。
_Avoid_: 任意文件路径、GPU 纹理句柄

**场景代理**：
独立持有提供者身份、场景事务和绘制资源，并向一个或多个渲染器发布完整场景的运行时。
_Avoid_: Decky 后端、渲染器、数据采集器

**应用身份**：
标识同一个绘制提供者应用的稳定名称，用于保存授权、层级和可见性设置。
_Avoid_: 进程 ID、连接 ID

**实例身份**：
标识绘制提供者某次运行实例的临时身份；同一个应用可以同时拥有多个实例。
_Avoid_: 应用身份、持久设置键

**新提供者审批**：
要求未知应用身份先经用户同意才能显示画布的可选策略；默认关闭。
_Avoid_: IPC 身份校验、插件安装授权

**系统覆盖层**：
SteamOS 自带 MangoApp 在 Gamescope 中显示的性能覆盖层。
_Avoid_: Decky 页面、Steam Overlay

**系统统计内容**：
由 Steam/MangoHud 控制的原生性能统计画面，与提供者画布分别控制可见性。
_Avoid_: 扩展覆盖层、提供者画布

**扩展覆盖层**：
兼容系统覆盖层行为，并额外合成提供者画布的覆盖层。
_Avoid_: 替代 Gamescope、自定义桌面窗口

**游戏模式渲染器**：
在 SteamOS 游戏模式会话中呈现扩展覆盖层的渲染器。
_Avoid_: 项目唯一后端、桌面渲染器

**桌面游戏渲染器**：
在 SteamOS 桌面模式启动的游戏中呈现提供者画布的渲染器。
_Avoid_: KDE 桌面挂件、桌面常驻窗口

**桌面启动器**：
从当前活动运行时修订选择双 ABI 注入库，并把同一场景 socket 传播给原生、Steam Runtime 与 Proton 游戏的稳定启动入口。
_Avoid_: 每个兼容层一套渲染实现、KDE 常驻覆盖窗口

**安装状态**：
项目文件、活动版本和系统接入是否已持久化就绪；它不随一次进程或图形会话结束而改变。
_Avoid_: 运行状态、服务状态

**运行时修订**：
由运行时清单内容确定的不可变身份；同一个对外版本重新打包后会形成不同修订，以便原子切换和回滚。
_Avoid_: 插件版本、运行会话

**运行会话**：
一个渲染器从启动到正常停止或崩溃的生命周期。
_Avoid_: 安装、更新、卸载

**更新事务**：
把一个已验证运行时修订原子切换为活动修订，并保留失败回滚能力的持久化操作。
_Avoid_: 后端重载、进程重启

**待确认卸载**：
Decky 已调用卸载回调，但尚未确认这是用户删除还是更新过程中的临时移除。
_Avoid_: 已卸载、清理完成

**最终卸载**：
确认插件没有被新版本替换后，恢复系统覆盖层并删除项目所拥有文件的事务。
_Avoid_: `_unload`、休眠、关机、插件更新

**关闭扩展覆盖层**：
用户通过项目自身总开关停止显示第三方画布，但保留安装和管理能力。
_Avoid_: 禁用 Decky 插件、卸载

**禁用 Decky 插件**：
Loader 停止插件管理前后端的状态；它不代表关闭扩展覆盖层或删除运行时。
_Avoid_: 关闭覆盖层、最终卸载
