# 运行时按清单内容寻址

Decky 插件版本是面向用户和发布渠道的版本，不足以唯一标识运行时内容。开发测试可能在版本号尚未递增时重新打包；若目录只按版本命名，新包会拿新清单校验旧目录并无法激活，原地覆盖又会破坏更新失败回滚。

生命周期助手对规范化运行时清单计算 SHA-256 修订 ID，并以该 ID 创建不可变目录。安装状态同时记录对外版本与内部修订：相同修订的后端重载不重启，内容不同的同版本包执行完整更新事务，失败时恢复旧修订。旧版只记录版本号的状态在读取时把版本目录视为历史修订，因此现有安装可以直接迁移。
