# Decky 卸载回调只创建待确认卸载

Decky Loader 在正常插件更新时也会调用旧版本的 `_uninstall`，因此该回调无法证明用户选择了最终卸载。项目让 `_uninstall` 只持久化待确认卸载，并由插件目录之外的稳定生命周期助手延迟核对；新版本启动会取消记录，只有插件确实没有被替换时才恢复系统 MangoApp 并清理项目文件。`_unload`、休眠、关机和普通进程停止永远不进入这条事务。

